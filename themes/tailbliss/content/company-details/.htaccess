# Password protect the company details directory
AuthType Basic
AuthName "Restricted Area"
AuthUserFile /home/magcomau/.htpasswds/public_html/.htpasswd
Require user magptyltd

# Deny direct access to all PDF files
<FilesMatch "\.pdf$">
  Require user magptyltd
</FilesMatch>